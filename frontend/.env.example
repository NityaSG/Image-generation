# Base URL for the FastAPI backend.
# In dev, leave the default — Vite proxies /api -> http://localhost:8000.
# In production, set to the deployed backend origin (e.g. https://api.example.com).
VITE_API_BASE=/api
